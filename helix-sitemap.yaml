version: 1

sitemaps:
  website:
    origin: https://www.coca-colajourney.co.nz/
    lastmod: YYYY-MM-DD
    languages:
      default:
        source: /query-index.json
        destination: /sitemap.xml
        hreflang: en
        alternate: /{path}
